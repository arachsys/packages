diff --git a/bash-completion/mkfs b/bash-completion/mkfs
index 4edc6f0..36adf27 100644
--- a/bash-completion/mkfs
+++ b/bash-completion/mkfs
@@ -6,7 +6,7 @@ _mkfs_module()
 	prev="${COMP_WORDS[COMP_CWORD-1]}"
 	case $prev in
 		'-t'|'--type')
-			FSTYPES=$(for I in /sbin/mkfs.* /usr/sbin/mkfs.*; do if [ -e $I ]; then echo ${I##*mkfs.}; fi; done)
+			FSTYPES=$(for I in /bin/mkfs.*; do if [ -e $I ]; then echo ${I##*mkfs.}; fi; done)
 			COMPREPLY=( $(compgen -W "$FSTYPES" -- $cur) )
 			return 0
 			;;
diff --git a/bash-completion/setterm b/bash-completion/setterm
index dd46472..21ac4d4 100644
--- a/bash-completion/setterm
+++ b/bash-completion/setterm
@@ -8,7 +8,7 @@ _setterm_module()
 		'--term')
 			local TERM_LIST I
 			TERM_LIST=''
-			for I in /usr/share/terminfo/?/*; do
+			for I in /lib/terminfo/?/*; do
 				TERM_LIST+="${I##*/} "
 			done
 			COMPREPLY=( $(compgen -W "$TERM_LIST" -- $cur) )
diff --git a/bash-completion/ul b/bash-completion/ul
index 449cbe0..c885601 100644
--- a/bash-completion/ul
+++ b/bash-completion/ul
@@ -8,7 +8,7 @@ _ul_module()
 		'-t'|'--terminal')
 			local TERM_LIST I
 			TERM_LIST=''
-			for I in /usr/share/terminfo/?/*; do
+			for I in /lib/terminfo/?/*; do
 				TERM_LIST+="${I##*/} "
 			done
 			COMPREPLY=( $(compgen -W "$TERM_LIST" -- $cur) )
diff --git a/disk-utils/fsck.8 b/disk-utils/fsck.8
index c60eef2..1714ebc 100644
--- a/disk-utils/fsck.8
+++ b/disk-utils/fsck.8
@@ -81,7 +81,7 @@ In actuality,
 is simply a front-end for the various filesystem checkers
 (\fBfsck\fR.\fIfstype\fR) available under Linux.  The
 filesystem-specific checker is searched for in
-.I /sbin
+.I /bin
 first, then in
 .I /etc/fs
 and
@@ -418,9 +418,9 @@ The
 .B PATH
 environment variable is used to find filesystem checkers.  A set of
 system directories are searched first:
-.BR /sbin ,
-.BR /sbin/fs.d ,
-.BR /sbin/fs ,
+.BR /bin ,
+.BR /bin/fs.d ,
+.BR /bin/fs ,
 .BR /etc/fs ,
 and
 .BR /etc .
diff --git a/disk-utils/mkfs.8 b/disk-utils/mkfs.8
index 9a5cdc0..0d91539 100644
--- a/disk-utils/mkfs.8
+++ b/disk-utils/mkfs.8
@@ -31,16 +31,16 @@ is simply a front-end for the various filesystem builders
 available under Linux.
 The filesystem-specific builder is searched for in a number
 of directories, like perhaps
-.IR /sbin ,
-.IR /sbin/fs ,
-.IR /sbin/fs.d ,
+.IR /bin ,
+.IR /bin/fs ,
+.IR /bin/fs.d ,
 .IR /etc/fs ,
 .I /etc
 (the precise list is defined at compile time but at least
 contains
-.I /sbin
+.I /bin
 and
-.IR /sbin/fs ),
+.IR /bin/fs ),
 and finally in the directories
 listed in the PATH environment variable.
 Please see the filesystem-specific builder manual pages for
diff --git a/include/nls.h b/include/nls.h
index 3eabfe6..76791d9 100644
--- a/include/nls.h
+++ b/include/nls.h
@@ -4,7 +4,7 @@
 int main(int argc, char *argv[]);
 
 #ifndef LOCALEDIR
-#define LOCALEDIR "/usr/share/locale"
+#define LOCALEDIR "/lib/locale"
 #endif
 
 #ifdef HAVE_LOCALE_H
diff --git a/include/pathnames.h b/include/pathnames.h
index 9d2c22b..c98610d 100644
--- a/include/pathnames.h
+++ b/include/pathnames.h
@@ -18,15 +18,12 @@
 #define PATH_DELETED_SUFFIX	"\\040(deleted)"
 #define PATH_DELETED_SUFFIX_SZ	(sizeof(PATH_DELETED_SUFFIX) - 1)
 
-/* DEFPATHs from <paths.h> don't include /usr/local */
-#undef _PATH_DEFPATH
-#define	_PATH_DEFPATH	        "/usr/local/bin:/bin:/usr/bin"
-
-#undef _PATH_DEFPATH_ROOT
-#define	_PATH_DEFPATH_ROOT	"/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin"
+#ifndef _PATH_DEFPATH_ROOT
+# define _PATH_DEFPATH_ROOT _PATH_DEFPATH
+#endif
 
 #define _PATH_SECURETTY		"/etc/securetty"
-#define _PATH_WTMPLOCK		"/etc/wtmplock"
+#define _PATH_WTMPLOCK		"/var/log/wtmp.lock"
 
 #define	_PATH_HUSHLOGIN		".hushlogin"
 #define	_PATH_HUSHLOGINS	"/etc/hushlogins"
@@ -34,17 +31,17 @@
 #define _PATH_NOLOGIN_TXT	"/etc/nologin.txt"
 
 #ifndef _PATH_MAILDIR
-#define	_PATH_MAILDIR		"/var/spool/mail"
+#define	_PATH_MAILDIR		"/var/mail"
 #endif
 #define	_PATH_MOTDFILE		"/etc/motd"
 #define	_PATH_NOLOGIN		"/etc/nologin"
-#define	_PATH_VAR_NOLOGIN	"/var/run/nologin"
+#define	_PATH_VAR_NOLOGIN	"/run/nologin"
 
 #define _PATH_LOGIN		"/bin/login"
 #define _PATH_INITTAB		"/etc/inittab"
 #define _PATH_RC		"/etc/rc"
-#define _PATH_REBOOT		"/sbin/reboot"
-#define _PATH_SHUTDOWN		"/sbin/shutdown"
+#define _PATH_REBOOT		"/bin/reboot"
+#define _PATH_SHUTDOWN		"/bin/shutdown"
 #define _PATH_SINGLE		"/etc/singleboot"
 #define _PATH_SHUTDOWN_CONF	"/etc/shutdown.conf"
 
@@ -70,15 +67,15 @@
 /* used in term-utils/agetty.c */
 #define _PATH_ISSUE		"/etc/issue"
 #define _PATH_OS_RELEASE_ETC	"/etc/os-release"
-#define _PATH_OS_RELEASE_USR	"/usr/lib/os-release"
+#define _PATH_OS_RELEASE_USR	"/lib/os-release"
 
 #define _PATH_NUMLOCK_ON	_PATH_LOCALSTATEDIR "/numlock-on"
 
 #define _PATH_LOGINDEFS		"/etc/login.defs"
 
 /* used in misc-utils/look.c */
-#define _PATH_WORDS             "/usr/share/dict/words"
-#define _PATH_WORDS_ALT         "/usr/share/dict/web2"
+#define _PATH_WORDS             "/lib/dict/words"
+#define _PATH_WORDS_ALT         "/lib/dict/web2"
 
 /* mount paths */
 #define _PATH_UMOUNT		"/bin/umount"
@@ -114,7 +111,7 @@
 # ifdef MOUNTED					/* deprecated */
 #  define _PATH_MOUNTED		MOUNTED
 # else
-#  define _PATH_MOUNTED		"/etc/mtab"
+#  define _PATH_MOUNTED		"/run/mtab"
 # endif
 #endif
 
@@ -157,10 +154,10 @@
 #ifdef CONFIG_ADJTIME_PATH
 # define _PATH_ADJTIME		CONFIG_ADJTIME_PATH
 #else
-# define _PATH_ADJTIME		"/etc/adjtime"
+# define _PATH_ADJTIME		"/var/lib/hwclock/adjtime"
 #endif
 
-#define _PATH_LASTDATE		"/var/lib/lastdate"
+#define _PATH_LASTDATE		"/var/lib/hwclock/lastdate"
 #ifdef __ia64__
 # define _PATH_RTC_DEV		"/dev/efirtc"
 #else
diff --git a/include/pt-bsd.h b/include/pt-bsd.h
index 9bf47a5..8ef9eb6 100644
--- a/include/pt-bsd.h
+++ b/include/pt-bsd.h
@@ -8,7 +8,7 @@
 # define BSD_DISKMAGIC     ((uint32_t) 0x82564557)
 #endif
 
-#define BSD_LINUX_BOOTDIR "/usr/ucb/mdec"
+#define BSD_LINUX_BOOTDIR "/lib/mdec"
 
 #if defined (__alpha__) || defined (__powerpc__) || \
     defined (__ia64__) || defined (__hppa__)
diff --git a/libblkid/src/topology/dm.c b/libblkid/src/topology/dm.c
index e061632..3b7e39e 100644
--- a/libblkid/src/topology/dm.c
+++ b/libblkid/src/topology/dm.c
@@ -30,9 +30,7 @@ static int probe_dm_tp(blkid_probe pr,
 		const struct blkid_idmag *mag __attribute__((__unused__)))
 {
 	const char *paths[] = {
-		"/usr/local/sbin/dmsetup",
-		"/usr/sbin/dmsetup",
-		"/sbin/dmsetup"
+		"/bin/dmsetup"
 	};
 	int dmpipe[] = { -1, -1 }, stripes, stripesize;
 	char *cmd = NULL;
diff --git a/libblkid/src/topology/lvm.c b/libblkid/src/topology/lvm.c
index bd079d4..508fa25 100644
--- a/libblkid/src/topology/lvm.c
+++ b/libblkid/src/topology/lvm.c
@@ -36,9 +36,7 @@ static int probe_lvm_tp(blkid_probe pr,
 		const struct blkid_idmag *mag __attribute__((__unused__)))
 {
 	const char *paths[] = {
-		"/usr/local/sbin/lvdisplay",
-		"/usr/sbin/lvdisplay",
-		"/sbin/lvdisplay"
+		"/bin/lvdisplay"
 	};
 	int lvpipe[] = { -1, -1 }, stripes = 0, stripesize = 0;
 	FILE *stream = NULL;
diff --git a/libmount/python/context.c b/libmount/python/context.c
index 6232394..246cbdf 100644
--- a/libmount/python/context.c
+++ b/libmount/python/context.c
@@ -332,7 +332,7 @@ static PyObject *Context_disable_canonicalize(ContextObjext *self, PyObject *arg
 }
 
 #define Context_disable_helpers_HELP "disable_helpers(disable)\n\n" \
-	"Enable/disable /sbin/[u]mount.* helpers (see mount(8) man page, option -i).\n" \
+	"Enable/disable /bin/[u]mount.* helpers (see mount(8) man page, option -i).\n" \
 	"\n" \
 	"Returns self or raises an exception in case of an error."
 static PyObject *Context_disable_helpers(ContextObjext *self, PyObject *args, PyObject *kwds)
@@ -906,7 +906,7 @@ static PyObject *Context_do_mount(ContextObjext *self)
 }
 
 #define Context_do_umount_HELP "do_umount()\n\n" \
-	"Umount filesystem by umount(2) or fork()+exec(/sbin/umount.type).\n" \
+	"Umount filesystem by umount(2) or fork()+exec(/bin/umount.type).\n" \
 	"Unnecessary for Cxt.umount().\n" \
 	"\n" \
 	"See also Cxt.disable_helpers().\n" \
@@ -924,7 +924,7 @@ static PyObject *Context_do_umount(ContextObjext *self)
 }
 
 #define Context_mount_HELP "mount()\n\n" \
-	"High-level, mounts filesystem by mount(2) or fork()+exec(/sbin/mount.type).\n" \
+	"High-level, mounts filesystem by mount(2) or fork()+exec(/bin/mount.type).\n" \
 	"\n" \
 	"This is similar to:\n" \
 	"\n" \
@@ -952,7 +952,7 @@ static PyObject *Context_mount(ContextObjext *self)
 }
 
 #define Context_umount_HELP "umount()\n\n" \
-	"High-level, umounts filesystem by umount(2) or fork()+exec(/sbin/umount.type).\n" \
+	"High-level, umounts filesystem by umount(2) or fork()+exec(/bin/umount.type).\n" \
 	"\n" \
 	"This is similar to:\n" \
 	"\n" \
diff --git a/libmount/src/context.c b/libmount/src/context.c
index 8d19dc2..0a26529 100644
--- a/libmount/src/context.c
+++ b/libmount/src/context.c
@@ -482,7 +482,7 @@ int mnt_context_is_rdonly_umount(struct libmnt_context *cxt)
  * @cxt: mount context
  * @disable: TRUE or FALSE
  *
- * Enable/disable /sbin/[u]mount.* helpers (see mount(8) man page, option -i).
+ * Enable/disable /bin/[u]mount.* helpers (see mount(8) man page, option -i).
  *
  * Returns: 0 on success, negative number in case of error.
  */
diff --git a/libmount/src/context_umount.c b/libmount/src/context_umount.c
index 2a39a6f..05d5131 100644
--- a/libmount/src/context_umount.c
+++ b/libmount/src/context_umount.c
@@ -229,7 +229,7 @@ static int lookup_umount_fs(struct libmnt_context *cxt)
 	 * mountinfo about all mountpoints although we look for only one entry.
 	 *
 	 * All we need is fstype and to check if there is no userspace mount
-	 * options for the target (e.g. helper=udisks to call /sbin/umount.udisks).
+	 * options for the target (e.g. helper=udisks to call /bin/umount.udisks).
 	 *
 	 * So, let's use statfs() if possible (it's bad idea for --lazy/--force
 	 * umounts as target is probably unreachable NFS, also for --detach-loop
@@ -389,7 +389,7 @@ static int evaluate_permissions(struct libmnt_context *cxt)
 		if (rc)
 			return rc;
 		if (cxt->helper)
-			return 0;	/* we'll call /sbin/umount.<uhelper> */
+			return 0;	/* we'll call /bin/umount.<uhelper> */
 	}
 
 	/*
@@ -799,7 +799,7 @@ int mnt_context_prepare_umount(struct libmnt_context *cxt)
  * mnt_context_do_umount:
  * @cxt: mount context
  *
- * Umount filesystem by umount(2) or fork()+exec(/sbin/umount.type).
+ * Umount filesystem by umount(2) or fork()+exec(/bin/umount.type).
  * Unnecessary for mnt_context_umount().
  *
  * See also mnt_context_disable_helpers().
@@ -884,7 +884,7 @@ int mnt_context_finalize_umount(struct libmnt_context *cxt)
  * mnt_context_umount:
  * @cxt: umount context
  *
- * High-level, umounts filesystem by umount(2) or fork()+exec(/sbin/umount.type).
+ * High-level, umounts filesystem by umount(2) or fork()+exec(/bin/umount.type).
  *
  * This is similar to:
  *
diff --git a/libmount/src/libmount.h b/libmount/src/libmount.h
index 833c65e..481dfba 100644
--- a/libmount/src/libmount.h
+++ b/libmount/src/libmount.h
@@ -44,7 +44,7 @@ struct libmnt_cache;
 /**
  * libmnt_lock:
  *
- * Stores information about the locked file (e.g. /etc/mtab)
+ * Stores information about the locked file (e.g. /run/mtab)
  */
 struct libmnt_lock;
 
@@ -95,7 +95,7 @@ struct libmnt_table;
 /**
  * libmnt_update
  *
- * /etc/mtab or utab update description
+ * /run/mtab or utab update description
  */
 struct libmnt_update;
 
diff --git a/libmount/src/libmount.h.in b/libmount/src/libmount.h.in
index 2338cc6..cfbd9a3 100644
--- a/libmount/src/libmount.h.in
+++ b/libmount/src/libmount.h.in
@@ -44,7 +44,7 @@ struct libmnt_cache;
 /**
  * libmnt_lock:
  *
- * Stores information about the locked file (e.g. /etc/mtab)
+ * Stores information about the locked file (e.g. /run/mtab)
  */
 struct libmnt_lock;
 
@@ -95,7 +95,7 @@ struct libmnt_table;
 /**
  * libmnt_update
  *
- * /etc/mtab or utab update description
+ * /run/mtab or utab update description
  */
 struct libmnt_update;
 
diff --git a/libmount/src/lock.c b/libmount/src/lock.c
index 0d94329..a384faa 100644
--- a/libmount/src/lock.c
+++ b/libmount/src/lock.c
@@ -330,7 +330,7 @@ static int mnt_wait_mtab_lock(struct libmnt_lock *ml, struct flock *fl, time_t m
  * backwardly compatible code.
  *
  * Don't forget that this code has to be compatible with 3rd party mounts
- * (/sbin/mount.foo) and has to work with NFS.
+ * (/bin/mount.foo) and has to work with NFS.
  * -- kzak@redhat.com [May-2009]
  */
 
diff --git a/libmount/src/mountP.h b/libmount/src/mountP.h
index d28eb9c..210e06e 100644
--- a/libmount/src/mountP.h
+++ b/libmount/src/mountP.h
@@ -306,7 +306,7 @@ struct libmnt_context
 
 	int	flags;		/* private context flags */
 
-	char	*helper;	/* name of the used /sbin/[u]mount.<type> helper */
+	char	*helper;	/* name of the used /bin/[u]mount.<type> helper */
 	int	helper_status;	/* helper wait(2) status */
 	int	helper_exec_status; /* 1: not called yet, 0: success, <0: -errno */
 
diff --git a/libmount/src/optmap.c b/libmount/src/optmap.c
index 3ad7727..6ca2292 100644
--- a/libmount/src/optmap.c
+++ b/libmount/src/optmap.c
@@ -168,9 +168,9 @@ static const struct libmnt_optmap userspace_opts_map[] =
 
    { "nofail",  MNT_MS_NOFAIL, MNT_NOMTAB },               /* Do not fail if ENOENT on dev */
 
-   { "uhelper=", MNT_MS_UHELPER },			   /* /sbin/umount.<helper> */
+   { "uhelper=", MNT_MS_UHELPER },			   /* /bin/umount.<helper> */
 
-   { "helper=", MNT_MS_HELPER },			   /* /sbin/mount.<helper> */
+   { "helper=", MNT_MS_HELPER },			   /* /bin/mount.<helper> */
 
    { NULL, 0, 0 }
 };
diff --git a/libuuid/src/uuidd.h b/libuuid/src/uuidd.h
index 2f70968..6a9b8f6 100644
--- a/libuuid/src/uuidd.h
+++ b/libuuid/src/uuidd.h
@@ -38,7 +38,7 @@
 #define UUIDD_DIR		_PATH_LOCALSTATEDIR "/uuidd"
 #define UUIDD_SOCKET_PATH	UUIDD_DIR "/request"
 #define UUIDD_PIDFILE_PATH	UUIDD_DIR "/uuidd.pid"
-#define UUIDD_PATH		"/usr/sbin/uuidd"
+#define UUIDD_PATH		"/bin/uuidd"
 
 #define UUIDD_OP_GETPID			0
 #define UUIDD_OP_GET_MAXOP		1
diff --git a/login-utils/login.1 b/login-utils/login.1
index b3f3d3f..ee97952 100644
--- a/login-utils/login.1
+++ b/login-utils/login.1
@@ -53,10 +53,8 @@ and
 are set according to the appropriate fields in the password entry.
 .B $PATH
 defaults to
-.I /usr\:/local\:/bin:\:/bin:\:/usr\:/bin
-for normal users, and to
-.I /usr\:/local\:/sbin:\:/usr\:/local\:/bin:\:/sbin:\:/bin:\:/usr\:/sbin:\:/usr\:/bin
-for root, if not otherwise configured.
+.I /bin
+for all users.
 .P
 The environment variable
 .B $TERM
@@ -257,7 +255,7 @@ user enters her password instead of her login name.
 .RS 4
 If set, it will be used to define the PATH environment variable when
 a regular user logs in.  The default value is
-.I /usr\:/local\:/bin:\:/bin:\:/usr\:/bin
+.I /bin
 .RE
 .PP
 .B ENV_ROOTPATH
@@ -268,17 +266,17 @@ a regular user logs in.  The default value is
 .RS 4
 If set, it will be used to define the PATH environment variable when
 the superuser logs in.  The default value is
-.I /usr\:/local\:/sbin:\:/usr\:/local\:/bin:\:/sbin:\:/bin:\:/usr\:/sbin:\:/usr\:/bin
+.I /bin
 .RE
 .SH FILES
 .nf
-.I /var/run/utmp
+.I /run/utmp
 .I /var/log/wtmp
 .I /var/log/lastlog
-.I /var/spool/mail/*
+.I /var/mail/*
 .I /etc/motd
 .I /etc/passwd
-.I /etc/nologin
+.I /run/nologin
 .I /etc/pam.d/login
 .I /etc/pam.d/remote
 .I /etc/hushlogins
diff --git a/login-utils/runuser.1 b/login-utils/runuser.1
index 0672cd6..e0b3c2e 100644
--- a/login-utils/runuser.1
+++ b/login-utils/runuser.1
@@ -159,7 +159,7 @@ for
 .RS 4
 Defines the PATH environment variable for a regular user.  The
 default value is
-.IR /usr/local/bin:\:/bin:\:/usr/bin .
+.IR /bin .
 .RE
 .PP
 .B ENV_ROOTPATH
@@ -169,7 +169,7 @@ default value is
 (string)
 .RS 4
 Defines the PATH environment variable for root. The default value is
-.IR /usr/local/sbin:\:/usr/local/bin:\:/sbin:\:/bin:\:/usr/sbin:\:/usr/bin .
+.IR /bin .
 .RE
 .PP
 .B ALWAYS_SET_PATH
diff --git a/login-utils/su.1 b/login-utils/su.1
index f5fecf1..89804b5 100644
--- a/login-utils/su.1
+++ b/login-utils/su.1
@@ -165,7 +165,7 @@ a non-negative integer.
 .RS 4
 Defines the PATH environment variable for a regular user.  The
 default value is
-.IR /usr/local/bin:\:/bin:\:/usr/bin .
+.IR /bin .
 .RE
 .PP
 .B ENV_ROOTPATH
@@ -175,7 +175,7 @@ default value is
 (string)
 .RS 4
 Defines the PATH environment variable for root.  The default value is
-.IR /usr/local/sbin:\:/usr/local/bin:\:/sbin:\:/bin:\:/usr/sbin:\:/usr/bin .
+.IR /bin .
 .RE
 .PP
 .B ALWAYS_SET_PATH
diff --git a/login-utils/sulogin.c b/login-utils/sulogin.c
index 51bc582..08a9ff6 100644
--- a/login-utils/sulogin.c
+++ b/login-utils/sulogin.c
@@ -87,7 +87,7 @@ static volatile sig_atomic_t sigchild;
  */
 static int plymouth_command(const char* arg)
 {
-	const char *cmd = "/usr/bin/plymouth";
+	const char *cmd = "/bin/plymouth";
 	static int has_plymouth = 1;
 	pid_t pid;
 
diff --git a/misc-utils/findmnt.8 b/misc-utils/findmnt.8
index 31c614a..52b19cb 100644
--- a/misc-utils/findmnt.8
+++ b/misc-utils/findmnt.8
@@ -21,7 +21,7 @@ will list all mounted filesytems or search for a filesystem.  The
 .B findmnt
 command is able to search in
 .IR /etc/fstab ,
-.IR /etc/mtab
+.IR /run/mtab
 or
 .IR /proc/self/mountinfo .
 If
@@ -98,7 +98,7 @@ source file (the option \fB\-F\fP) is specified.
 .TP
 .BR \-m , " \-\-mtab"
 Search in
-.IR /etc/mtab .
+.IR /run/mtab .
 The output is in the list format (see \fB\-\-list\fP).
 .TP
 .BR \-N , " \-\-task \fItid\fP"
diff --git a/misc-utils/getopt.1 b/misc-utils/getopt.1
index da7207f..7f78b15 100644
--- a/misc-utils/getopt.1
+++ b/misc-utils/getopt.1
@@ -415,11 +415,9 @@ if it is called with
 .SH EXAMPLES
 Example scripts for (ba)sh and (t)csh are provided with the
 .BR getopt (1)
-distribution, and are optionally installed in
-.BR /usr/share/getopt/
-or
-.BR /usr/share/doc/
-in the util-linux subdirectory.
+distribution, and are optionally installed in the
+.BR /lib/doc/getopt/
+directory.
 .SH ENVIRONMENT
 .IP POSIXLY_CORRECT
 This environment variable is examined by the
diff --git a/misc-utils/kill.1 b/misc-utils/kill.1
index 0d430ef..3dc2474 100644
--- a/misc-utils/kill.1
+++ b/misc-utils/kill.1
@@ -78,7 +78,7 @@ The signal to send.  It may be given as a name or a number.
 \fB\-l\fR, \fB\-\-list\fR [\fInumber\fR]
 Print a list of signal names, or convert the given signal number to a name.
 The signals can be found in
-.I /usr/\:include/\:linux/\:signal.h
+.I /lib/\:include/\:linux/\:signal.h
 .TP
 \fB\-L\fR, \fB\-\-table\fR
 Similar to \fB\-l\fR, but it will print signal names and their corresponding
diff --git a/misc-utils/look.1 b/misc-utils/look.1
index e96a431..c5cfb4e 100644
--- a/misc-utils/look.1
+++ b/misc-utils/look.1
@@ -59,7 +59,7 @@ is invoked with).
 If
 .I file
 is not specified, the file
-.I /usr/share/dict/words
+.I /lib/dict/words
 is used, only alphanumeric characters are compared and the case of
 alphabetic characters is ignored.
 .SH OPTIONS
@@ -98,9 +98,9 @@ look -t: root:foobar /tmp/look.dict
 .nf
 .RE
 .SH FILES
-.IP "\fB/usr/share/dict/words\fR" 4
+.IP "\fB/lib/dict/words\fR" 4
 the dictionary
-.IP "\fB/usr/share/dict/web2\fR" 4
+.IP "\fB/lib/dict/web2\fR" 4
 the alternative dictionary
 .SH "SEE ALSO"
 .BR grep (1),
diff --git a/misc-utils/uuidd.rc.in b/misc-utils/uuidd.rc.in
index 26273e5..84304a2 100644
--- a/misc-utils/uuidd.rc.in
+++ b/misc-utils/uuidd.rc.in
@@ -13,8 +13,8 @@
 #
 set -e
 
-PATH=/bin:/usr/bin:/sbin:/usr/sbin
-DAEMON=/usr/sbin/uuidd
+PATH=/bin
+DAEMON=/bin/uuidd
 UUIDD_USER=uuidd
 UUIDD_GROUP=uuidd
 UUIDD_DIR=@localstatedir@/uuidd
diff --git a/misc-utils/whereis.1 b/misc-utils/whereis.1
index 6cdec70..46ff025 100644
--- a/misc-utils/whereis.1
+++ b/misc-utils/whereis.1
@@ -72,11 +72,11 @@ The options \fB\-B\fP, \fB\-M\fP and \fB\-S\fP reset search paths for the
 subsequent \fIname\fP patterns.  For example,
 .RS
 .sp
-.B "whereis -m ls -M /usr/share/man/man1 -f cal"
+.B "whereis -m ls -M /lib/man/man1 -f cal"
 .sp
 .RE
 searches for "ls" man pages in all default paths, but for "cal" in
-the /usr/share/man/man1 directory only.
+the /lib/man/man1 directory only.
 
 .SH OPTIONS
 .TP
@@ -127,16 +127,16 @@ is specified, the option will output the hard-coded paths
 that the command was able to find on the system.
 .SH EXAMPLE
 To find all files in
-.B /usr/\:bin
+.B /bin
 which are not documented
 in
-.B /usr/\:man/\:man1
+.B /lib/\:man/\:man1
 or have no source in
-.BR /usr/\:src :
+.BR /lib/\:src :
 .IP
-.B cd /usr/bin
+.B cd /bin
 .br
-.B whereis \-u \-ms \-M /usr/man/man1 \-S /usr/src \-f *
+.B whereis \-u \-ms \-M /lib/man/man1 \-S /lib/src \-f *
 .SH "FILE SEARCH PATHS"
 By default
 .B whereis
diff --git a/misc-utils/whereis.c b/misc-utils/whereis.c
index 9fc5ee7..4e5d066 100644
--- a/misc-utils/whereis.c
+++ b/misc-utils/whereis.c
@@ -94,77 +94,20 @@ struct wh_dirlist {
 };
 
 static const char *bindirs[] = {
-	"/usr/bin",
-	"/usr/sbin",
-	"/usr/lib",
-	"/usr/lib64",
 	"/bin",
-	"/sbin",
 	"/etc",
-	"/usr/etc",
 	"/lib",
-	"/lib64",
-	"/usr/games",
-	"/usr/games/bin",
-	"/usr/games/lib",
-	"/usr/emacs/etc",
-	"/usr/lib/emacs/*/etc",
-	"/usr/TeX/bin",
-	"/usr/tex/bin",
-	"/usr/interviews/bin/LINUX",
-
-	"/usr/X11R6/bin",
-	"/usr/X386/bin",
-	"/usr/bin/X11",
-	"/usr/X11/bin",
-	"/usr/X11R5/bin",
-
-	"/usr/local/bin",
-	"/usr/local/sbin",
-	"/usr/local/etc",
-	"/usr/local/lib",
-	"/usr/local/games",
-	"/usr/local/games/bin",
-	"/usr/local/emacs/etc",
-	"/usr/local/TeX/bin",
-	"/usr/local/tex/bin",
-	"/usr/local/bin/X11",
-
-	"/usr/contrib",
-	"/usr/hosts",
-	"/usr/include",
-
-	"/usr/g++-include",
-
-	"/usr/ucb",
-	"/usr/old",
-	"/usr/new",
-	"/usr/local",
-	"/usr/libexec",
-	"/usr/share",
-
-	"/opt/*/bin",
+	"/lib/*",
 	NULL
 };
 
 static const char *mandirs[] = {
-	"/usr/man/*",
-	"/usr/share/man/*",
-	"/usr/X386/man/*",
-	"/usr/X11/man/*",
-	"/usr/TeX/man/*",
-	"/usr/interviews/man/mann",
-	"/usr/share/info",
+	"/lib/man/*",
 	NULL
 };
 
 static const char *srcdirs[] = {
-	"/usr/src/*",
-	"/usr/src/lib/libc/*",
-	"/usr/src/lib/libc/net/*",
-	"/usr/src/ucb/pascal",
-	"/usr/src/ucb/pascal/utilities",
-	"/usr/src/undoc",
+	"/lib/src/*",
 	NULL
 };
 
diff --git a/sys-utils/eject.1 b/sys-utils/eject.1
index edc6a2a..588adb7 100644
--- a/sys-utils/eject.1
+++ b/sys-utils/eject.1
@@ -73,7 +73,7 @@ The option tells eject to not try to unmount at all.
 With this option the selected device is displayed but no action is performed.
 .TP
 .BR \-p , " \-\-proc"
-This option allows you to use /proc/mounts instead /etc/mtab.  It also passes the
+This option allows you to use /proc/mounts instead /run/mtab.  It also passes the
 \fB\-n\fR option to \fBumount\fR(8).
 .TP
 .BR \-q , " \-\-tape"
diff --git a/sys-utils/eject.c b/sys-utils/eject.c
index 4f7d6e6..a1d3274 100644
--- a/sys-utils/eject.c
+++ b/sys-utils/eject.c
@@ -146,7 +146,7 @@ static void __attribute__ ((__noreturn__)) usage(FILE * out)
 		" -m, --no-unmount            do not unmount device even if it is mounted\n"
 		" -M, --no-partitions-unmount do not unmount another partitions\n"
 		" -n, --noop                  don't eject, just show device found\n"
-		" -p, --proc                  use /proc/mounts instead of /etc/mtab\n"
+		" -p, --proc                  use /proc/mounts instead of /run/mtab\n"
 		" -q, --tape                  eject tape\n"
 		" -r, --cdrom                 eject CD-ROM\n"
 		" -s, --scsi                  eject SCSI device\n"
diff --git a/sys-utils/hwclock.8 b/sys-utils/hwclock.8
index f559e88..c42d8dc 100644
--- a/sys-utils/hwclock.8
+++ b/sys-utils/hwclock.8
@@ -1008,7 +1008,7 @@ known to have issues.
 .PP
 To configure a system to use a particular database all of the files
 located in its directory must be copied to the root of
-.IR \%/usr/share/zoneinfo .
+.IR \%/lib/zoneinfo .
 Files are never used directly from the posix or 'right' subdirectories, e.g.,
 .RI \%TZ=' right/Europe/Dublin '.
 This habit was becoming so common that the upstream zoneinfo project
@@ -1016,11 +1016,11 @@ restructured the system's file tree by moving the posix and 'right'
 subdirectories out of the zoneinfo directory and into sibling directories:
 .PP
 .in +2
-.I /usr/share/zoneinfo
+.I /lib/zoneinfo
 .br
-.I /usr/share/zoneinfo\-posix
+.I /lib/zoneinfo\-posix
 .br
-.I /usr/share/zoneinfo\-leaps
+.I /lib/zoneinfo\-leaps
 .PP
 Unfortunately, some Linux distributions are changing it back to the old
 tree structure in their packages. So the problem of system
@@ -1031,7 +1031,7 @@ when an application such as a World Clock needs the South_Pole timezone
 file; or an email MTA, or
 .B hwclock
 needs the UTC timezone file; they fetch it from the root of
-.I \%/usr/share/zoneinfo
+.I \%/lib/zoneinfo
 , because that is what they are supposed to do. Those files exclude leap
 seconds, but the System Clock now includes them, causing an incorrect
 time conversion.
diff --git a/sys-utils/hwclock.c b/sys-utils/hwclock.c
index c9d422d..458cb33 100644
--- a/sys-utils/hwclock.c
+++ b/sys-utils/hwclock.c
@@ -799,7 +799,7 @@ static int interpret_date_string(const char *date_opt, time_t * const time_p)
  * Set the System Clock to time 'newtime'.
  *
  * Also set the kernel time zone value to the value indicated by the TZ
- * environment variable and/or /usr/lib/zoneinfo/, interpreted as tzset()
+ * environment variable and/or /lib/zoneinfo/, interpreted as tzset()
  * would interpret them.
  *
  * If this is the first call of settimeofday since boot, then this also sets
@@ -883,7 +883,7 @@ set_system_clock(const bool hclock_valid, const struct timeval newtime,
  * and the timezone unless universal is TRUE.
  *
  * Also set the kernel time zone value to the value indicated by the TZ
- * environment variable and/or /usr/lib/zoneinfo/, interpreted as tzset()
+ * environment variable and/or /lib/zoneinfo/, interpreted as tzset()
  * would interpret them.
  *
  * If 'testing' is true, don't actually update anything -- just say we would
@@ -1991,7 +1991,7 @@ void __attribute__((__noreturn__)) hwaudit_exit(int status)
  * 98.06.20 BJH Version 2.3.
  *
  * Make --hctosys set the kernel timezone from TZ environment variable
- * and/or /usr/lib/zoneinfo. From Klaus Ripke (klaus@ripke.com).
+ * and/or /lib/zoneinfo. From Klaus Ripke (klaus@ripke.com).
  *
  * 98.03.05 BJH. Version 2.2.
  *
diff --git a/sys-utils/mount.8 b/sys-utils/mount.8
index a4ff749..94d2f6e 100644
--- a/sys-utils/mount.8
+++ b/sys-utils/mount.8
@@ -182,7 +182,7 @@ is less fortunate: the error message `none busy' from
 can be confusing.)
 .RE
 
-.B The /etc/fstab, /etc/mtab and /proc/mounts files.
+.B The /etc/fstab, /run/mtab and /proc/mounts files.
 .RS
 The file
 .I /etc/fstab
@@ -226,7 +226,7 @@ The programs
 and
 .B umount
 traditionally maintained a list of currently mounted filesystems in the file
-.IR /etc/mtab .
+.IR /run/mtab .
 This real mtab file is still supported, but on current Linux systems it is
 better to make it a symlink to
 .I /proc/mounts
@@ -378,11 +378,11 @@ changed by a separate remount command, for example:
 .I newdir
 .RE
 
-Note that the behavior of the remount operation depends on the /etc/mtab file.
-The first command stores the 'bind' flag in the /etc/mtab file and the second
+Note that the behavior of the remount operation depends on the /run/mtab file.
+The first command stores the 'bind' flag in the /run/mtab file and the second
 command reads the flag from the file.  If you have a system without the
-/etc/mtab file or if you explicitly define source and target for the remount
-command (then \fBmount\fR(8) does not read /etc/mtab), then you have to use
+/run/mtab file or if you explicitly define source and target for the remount
+command (then \fBmount\fR(8) does not read /run/mtab), then you have to use
 the bind flag (or option) for the remount command too.  For example:
 
 .RS
@@ -525,7 +525,7 @@ argument, and finally applying a
 option, when present.
 
 The command \fBmount\fR does not pass all command-line options to the
-\fB/sbin/mount.\fIsuffix\fR mount helpers.  The interface between \fBmount\fR
+\fB/bin/mount.\fIsuffix\fR mount helpers.  The interface between \fBmount\fR
 and the mount helpers is described below in the section EXTERNAL HELPERS.
 .sp
 Command-line options available for the
@@ -554,7 +554,7 @@ helpers which call \fBmount -i\fR.  It is strongly recommended to not use this
 command-line option for normal mount operations.
 .sp
 Note that \fBmount\fR(8) does not pass this option to the
-\fB/sbin/mount.\fItype\fR helpers.
+\fB/bin/mount.\fItype\fR helpers.
 .TP
 .BR \-F , " \-\-fork"
 (Used in conjunction with
@@ -565,9 +565,9 @@ in parallel.
 This has the advantage that it is faster; also NFS timeouts go in
 parallel.  A disadvantage is that the mounts are done in undefined order.
 Thus, you cannot use this option if you want to mount both
-.I /usr
+.I /foo
 and
-.IR /usr/spool .
+.IR /foo/bar .
 .IP "\fB\-f, \-\-fake\fP"
 Causes everything to be done except for the actual system call; if it's not
 obvious, this ``fakes'' mounting the filesystem.  This option is useful in
@@ -577,10 +577,10 @@ flag to determine what the
 .B mount
 command is trying to do.  It can also be used to add entries for devices
 that were mounted earlier with the \fB\-n\fR option.  The \fB\-f\fR option
-checks for an existing record in /etc/mtab and fails when the record already
+checks for an existing record in /run/mtab and fails when the record already
 exists (with a regular non-fake mount, this check is done by the kernel).
 .IP "\fB\-i, \-\-internal\-only\fP"
-Don't call the \fB/sbin/mount.\fIfilesystem\fR helper even if it exists.
+Don't call the \fB/bin/mount.\fIfilesystem\fR helper even if it exists.
 .TP
 .BR \-L , " \-\-label " \fIlabel
 Mount the partition that has the specified
@@ -601,7 +601,7 @@ Move a subtree to some other place.  See above.
 .TP
 .BR \-n , " \-\-no\-mtab"
 Mount without writing in
-.IR /etc/mtab .
+.IR /run/mtab .
 This is necessary for example when
 .I /etc
 is on a read-only filesystem.
@@ -707,7 +707,7 @@ or chroot scripts where additional configuration is specified beyond standard
 system configuration.
 .sp
 Note that \fBmount\fR(8) does not pass the option \fB\-\-fstab\fP to the
-\fB/sbin/mount.\fItype\fR helpers, meaning that the alternative fstab files will be
+\fB/bin/mount.\fItype\fR helpers, meaning that the alternative fstab files will be
 invisible for the helpers.  This is no problem for normal mounts, but user
 (non-root) mounts always require fstab to verify the user's rights.
 .TP
@@ -804,13 +804,13 @@ For a few types however (like nfs, nfs4, cifs, smbfs, ncpfs) an ad hoc code is
 necessary.  The nfs, nfs4, cifs, smbfs, and ncpfs filesystems
 have a separate mount program.  In order to make it possible to
 treat all types in a uniform way, \fBmount\fR will execute the program
-.BI /sbin/mount. type
+.BI /bin/mount. type
 (if that exists) when called with type
 .IR type .
 Since different versions of the
 .B smbmount
 program have different calling conventions,
-.B /sbin/mount.smbfs
+.B /bin/mount.smbfs
 may have to be a shell script that sets up the desired call.
 .RE
 .TP
@@ -1186,7 +1186,7 @@ Set the owner and group of the files in the filesystem (default: uid=gid=0).
 Set the permission mask for ADFS 'owner' permissions and 'other' permissions,
 respectively (default: 0700 and 0077, respectively).
 See also
-.IR /usr/src/linux/Documentation/filesystems/adfs.txt .
+.IR /lib/src/linux/Documentation/filesystems/adfs.txt .
 .SH "Mount options for affs"
 .TP
 \fBuid=\fP\,\fIvalue\fP and \fBgid=\fP\,\fIvalue\fP
@@ -3099,7 +3099,7 @@ meaning that any loop device allocated by
 will be freed by
 .B umount
 independently of
-.IR /etc/mtab .
+.IR /run/mtab .
 
 You can also free a loop device by hand, using
 .BR "losetup \-d " or " umount \-d" .
@@ -3126,7 +3126,7 @@ bug
 user interrupt
 .TP
 .B 16
-problems writing or locking /etc/mtab
+problems writing or locking /run/mtab
 .TP
 .B 32
 mount failure
@@ -3142,7 +3142,7 @@ failed, some succeeded).
 The syntax of external mount helpers is:
 
 .RS
-.BI /sbin/mount. suffix
+.BI /bin/mount. suffix
 .I spec dir
 .RB [ \-sfnv ]
 .RB [ \-o
@@ -3154,7 +3154,7 @@ The syntax of external mount helpers is:
 where the \fIsuffix\fR is the filesystem type and the \fB\-sfnvo\fR options have
 the same meaning as the normal mount options.  The \fB\-t\fR option is used for
 filesystems with subtypes support (for example
-.BR "/sbin/mount.fuse \-t fuse.sshfs" ).
+.BR "/bin/mount.fuse \-t fuse.sshfs" ).
 
 The command \fBmount\fR does not pass the mount options
 .BR unbindable ,
@@ -3181,13 +3181,13 @@ comma-separated list as argument to the \fB\-o\fR option.
 .I /etc/fstab
 filesystem table
 .TP
-.I /etc/mtab
+.I /run/mtab
 table of mounted filesystems
 .TP
-.I /etc/mtab\s+3~\s0
+.I /run/mtab\s+3~\s0
 lock file
 .TP
-.I /etc/mtab.tmp
+.I /run/mtab.tmp
 temporary file
 .TP
 .I /etc/filesystems
@@ -3244,7 +3244,7 @@ for the
 .IR fatfs ).
 .PP
 It is possible that files
-.I /etc/mtab
+.I /run/mtab
 and
 .I /proc/mounts
 don't match on systems with regular mtab file. The first file is based only on
diff --git a/sys-utils/mount.c b/sys-utils/mount.c
index c650efe..527ec3e 100644
--- a/sys-utils/mount.c
+++ b/sys-utils/mount.c
@@ -373,7 +373,7 @@ static int mk_exit_code(struct libmnt_context *cxt, int rc)
 try_readonly:
 	if (mnt_context_helper_executed(cxt)) {
 		/*
-		 * /sbin/mount.<type> called, return status
+		 * /bin/mount.<type> called, return status
 		 */
 		if (rc == -MNT_ERR_APPLYFLAGS)
 			warnx(_("WARNING: failed to apply propagation flags"));
@@ -556,7 +556,7 @@ try_readonly:
 		if (mnt_fs_is_netfs(mnt_context_get_fs(cxt)))
 			fprintf(stderr, _(
 				"       (for several filesystems (e.g. nfs, cifs) you might\n"
-				"       need a /sbin/mount.<type> helper program)\n"));
+				"       need a /bin/mount.<type> helper program)\n"));
 
 		fprintf(stderr, _("\n"
 				"       In some cases useful info is found in syslog - try\n"
@@ -735,7 +735,7 @@ static void __attribute__((__noreturn__)) usage(FILE *out)
 	" -h, --help              display this help text and exit\n"
 	" -i, --internal-only     don't call the mount.<type> helpers\n"
 	" -l, --show-labels       lists all mounts with LABELs\n"
-	" -n, --no-mtab           don't write to /etc/mtab\n"));
+	" -n, --no-mtab           don't write to /run/mtab\n"));
 	fprintf(out, _(
 	" -o, --options <list>    comma-separated list of mount options\n"
 	" -O, --test-opts <list>  limit the set of filesystems (use with -a)\n"
diff --git a/sys-utils/pivot_root.8 b/sys-utils/pivot_root.8
index 525eb43..72d9700 100644
--- a/sys-utils/pivot_root.8
+++ b/sys-utils/pivot_root.8
@@ -61,7 +61,7 @@ mount -o ro 10.0.0.1:/my_root /mnt
 killall portmap   # portmap keeps old root busy
 cd /mnt
 pivot_root . old_root
-exec chroot . sh -c 'umount /old_root; exec /sbin/init' \\
+exec chroot . sh -c 'umount /old_root; exec /bin/init' \\
   <dev/console >dev/console 2>&1
 .fi
 .SH "SEE ALSO"
diff --git a/sys-utils/readprofile.8 b/sys-utils/readprofile.8
index c01e3ca..95b2ef4 100644
--- a/sys-utils/readprofile.8
+++ b/sys-utils/readprofile.8
@@ -42,7 +42,7 @@ only the decimal number is printed.
 .TP
 \fB\-m\fR, \fB\-\-mapfile\fR \fImapfile\fR
 Specify a mapfile, which by default is
-.BR /usr/src/linux/System.map .
+.BR /lib/src/linux/System.map .
 You should specify the map file on cmdline if your current kernel
 isn't the last one you compiled, or if you keep System.map elsewhere.
 If the name of the map file ends with `.gz' it is decompressed on the
@@ -142,8 +142,8 @@ out for misleading information.
 .SH FILES
 .nf
 /proc/profile              A binary snapshot of the profiling buffer.
-/usr/src/linux/System.map  The symbol table for the kernel.
-/usr/src/linux/*           The program being profiled :-)
+/lib/src/linux/System.map  The symbol table for the kernel.
+/lib/src/linux/*           The program being profiled :-)
 .fi
 .SH AVAILABILITY
 The readprofile command is part of the util-linux package and is
diff --git a/sys-utils/rtcwake.8 b/sys-utils/rtcwake.8
index c677586..0da3c7b 100644
--- a/sys-utils/rtcwake.8
+++ b/sys-utils/rtcwake.8
@@ -89,7 +89,7 @@ management.  This state operates similarly to Suspend-to-RAM, but includes a
 final step of writing memory contents to disk.
 .TP
 .B off
-ACPI state S5 (Poweroff).  This is done by calling '/sbin/shutdown'.
+ACPI state S5 (Poweroff).  This is done by calling '/bin/shutdown'.
 Not officially supported by ACPI, but it usually works.
 .TP
 .B no
@@ -156,4 +156,4 @@ There is NO WARRANTY, to the extent permitted by law.
 The rtcwake command is part of the util-linux package and is available from the
 .UR ftp://\:ftp.kernel.org\:/pub\:/linux\:/utils\:/util-linux/
 Linux Kernel Archive
-.UE .
\ No newline at end of file
+.UE .
diff --git a/sys-utils/swapon.8 b/sys-utils/swapon.8
index e571a5f..eda36a0 100644
--- a/sys-utils/swapon.8
+++ b/sys-utils/swapon.8
@@ -108,7 +108,7 @@ may also be used to skip non-existing device.
 
 .TP
 .BR \-f , " \-\-fixpgsz"
-Reinitialize (exec /sbin/mkswap) the swap space if its page size does not
+Reinitialize (exec /bin/mkswap) the swap space if its page size does not
 match that of the current running kernel.
 .BR mkswap (2)
 initializes the whole device and does not check for bad blocks.
diff --git a/sys-utils/swapon.c b/sys-utils/swapon.c
index 645a01d..0a039df 100644
--- a/sys-utils/swapon.c
+++ b/sys-utils/swapon.c
@@ -28,7 +28,7 @@
 #include "swapprober.h"
 #include "swapon-common.h"
 
-#define PATH_MKSWAP	"/sbin/mkswap"
+#define PATH_MKSWAP	"/bin/mkswap"
 
 #ifdef HAVE_SYS_SWAP_H
 # include <sys/swap.h>
diff --git a/sys-utils/tunelp.8 b/sys-utils/tunelp.8
index a69d88a..520b0a9 100644
--- a/sys-utils/tunelp.8
+++ b/sys-utils/tunelp.8
@@ -34,7 +34,7 @@ irq is handled by the parport driver.  You can change the parport irq for
 example via
 .IR /proc/parport/*/irq .
 Read
-.I /usr/src/linux/Documentation/parport.txt
+.I /lib/src/linux/Documentation/parport.txt
 for more details on parport.
 .TP
 \fB\-t\fR, \fB\-\-time\fR \fImilliseconds\fR
diff --git a/sys-utils/umount.8 b/sys-utils/umount.8
index 887d0ee..f060e8f 100644
--- a/sys-utils/umount.8
+++ b/sys-utils/umount.8
@@ -60,7 +60,7 @@ files.  A lazy unmount avoids this problem.
 .TP
 .BR \-a , " \-\-all"
 All of the filesystems described in
-.I /etc/mtab
+.I /run/mtab
 are unmounted, except the \fIproc\fR filesystem.
 .TP
 .BR \-A , " \-\-all\-targets"
@@ -68,14 +68,14 @@ Unmount all mountpoints in the current namespace for the specified filesystem.
 The filesystem can be specified by one of the mountpoints or the device name (or
 UUID, etc.).  When this option is used together with \fB\-\-recursive\fR, then
 all nested mounts within the filesystem are recursively unmounted.
-This option is only supported on systems where /etc/mtab is a symlink
+This option is only supported on systems where /run/mtab is a symlink
 to /proc/mounts.
 .TP
 .BR \-c , " \-\-no\-canonicalize"
 Do not canonicalize paths.  For more details about this option see the
 .BR mount (8)
 man page.  Note that \fBumount\fR does not pass this option to the
-.BI /sbin/umount. type
+.BI /bin/umount. type
 helpers.
 .TP
 .BR \-d , " \-\-detach\-loop"
@@ -85,7 +85,7 @@ device.
 .B \-\-fake
 Causes everything to be done except for the actual system call; this 'fakes'
 unmounting the filesystem.  It can be used to remove entries from
-.I /etc/mtab
+.I /run/mtab
 that were unmounted earlier with the
 .B \-n
 option.
@@ -95,7 +95,7 @@ Force an unmount (in case of an unreachable NFS system).  (Requires kernel
 2.1.116 or later.)
 .TP
 .BR \-i , " \-\-internal\-only"
-Do not call the \fB/sbin/umount.\fIfilesystem\fR helper even if it exists.
+Do not call the \fB/bin/umount.\fIfilesystem\fR helper even if it exists.
 By default such a helper program is called if it exists.
 .TP
 .BR \-l , " \-\-lazy"
@@ -105,7 +105,7 @@ anymore.  (Requires kernel 2.4.11 or later.)
 .TP
 .BR \-n , " \-\-no\-mtab"
 Unmount without writing in
-.IR /etc/mtab .
+.IR /run/mtab .
 .TP
 .BR \-O , " \-\-test\-opts " \fIoption\fR...
 Unmount only the filesystems that have the specified option set in
@@ -147,7 +147,7 @@ The
 .B umount
 command will free the loop device associated with a mount when
 it finds the option \fBloop=...\fR in
-.IR /etc/mtab ,
+.IR /run/mtab ,
 or when the \fB\-d\fR option was given.  Any still associated loop devices
 can be freed by using \fBlosetup -d\fR; see
 .BR losetup (8).
@@ -172,16 +172,16 @@ have subtype support.  For example:
 .RE
 .PP
 A \fBuhelper=\fIsomething\fR marker (unprivileged helper) can appear in
-the \fI/etc/mtab\fR file when ordinary users need to be able to unmount
+the \fI/run/mtab\fR file when ordinary users need to be able to unmount
 a mountpoint that is not defined in \fI/etc/fstab\fR
 (for example for a device that was mounted by \fBudisks\fR(1)).
 .PP
 A \fBhelper=\fItype\fR marker in the mtab file will redirect
 all unmount requests
-to the \fB/sbin/umount.\fItype\fR helper independently of UID.
+to the \fB/bin/umount.\fItype\fR helper independently of UID.
 .SH FILES
 .TP
-.B /etc/mtab
+.B /run/mtab
 table of mounted filesystems
 .TP
 .B /etc/fstab
diff --git a/sys-utils/umount.c b/sys-utils/umount.c
index 464c4c8..e442653 100644
--- a/sys-utils/umount.c
+++ b/sys-utils/umount.c
@@ -90,7 +90,7 @@ static void __attribute__((__noreturn__)) usage(FILE *out)
 	fputs(_("     --fake              dry run; skip the umount(2) syscall\n"), out);
 	fputs(_(" -f, --force             force unmount (in case of an unreachable NFS system)\n"), out);
 	fputs(_(" -i, --internal-only     don't call the umount.<type> helpers\n"), out);
-	fputs(_(" -n, --no-mtab           don't write to /etc/mtab\n"), out);
+	fputs(_(" -n, --no-mtab           don't write to /run/mtab\n"), out);
 	fputs(_(" -l, --lazy              detach the filesystem now, clean up things later\n"), out);
 	fputs(_(" -O, --test-opts <list>  limit the set of filesystems (use with -a)\n"), out);
 	fputs(_(" -R, --recursive         recursively unmount a target with all its children\n"), out);
@@ -186,7 +186,7 @@ static int mk_exit_code(struct libmnt_context *cxt, int rc)
 
 	if (mnt_context_helper_executed(cxt))
 		/*
-		 * /sbin/umount.<type> called, return status
+		 * /bin/umount.<type> called, return status
 		 */
 		return mnt_context_get_helper_status(cxt);
 
diff --git a/term-utils/agetty.8 b/term-utils/agetty.8
index 66f1a03..3828ce0 100644
--- a/term-utils/agetty.8
+++ b/term-utils/agetty.8
@@ -272,7 +272,7 @@ for the other fields.  See \fIinittab(5)\fP for more details.
 For a hardwired line or a console tty:
 
 .RS
-/sbin/agetty 9600 ttyS1
+/bin/agetty 9600 ttyS1
 .RE
 
 For a directly connected terminal without proper carrier-detect wiring
@@ -280,13 +280,13 @@ For a directly connected terminal without proper carrier-detect wiring
 prompt):
 
 .RS
-/sbin/agetty \-L 9600 ttyS1 vt100
+/bin/agetty \-L 9600 ttyS1 vt100
 .RE
 
 For an old-style dial-in line with a 9600/2400/1200 baud modem:
 
 .RS
-/sbin/agetty \-mt60 ttyS1 9600,2400,1200
+/bin/agetty \-mt60 ttyS1 9600,2400,1200
 .RE
 
 For a Hayes modem with a fixed 115200 bps interface to the machine
@@ -295,7 +295,7 @@ modem/computer DCD track modem/modem DCD, makes a DTR drop cause a
 disconnection, and turns on auto-answer after 1 ring):
 
 .RS
-/sbin/agetty \-w \-I 'ATE0Q1&D2&C1S0=1\\015' 115200 ttyS1
+/bin/agetty \-w \-I 'ATE0Q1&D2&C1S0=1\\015' 115200 ttyS1
 .RE
 
 .SH SECURITY NOTICE
@@ -339,7 +339,7 @@ See also \\S escape code.
 .TP
 S or S{VARIABLE}
 Insert the VARIABLE data from \fI/etc/os-release\fP, if the file does not exist
-then fallback to \fI/usr/lib/os-release\fP.  If the VARIABLE argument is not
+then fallback to \fI/lib/os-release\fP.  If the VARIABLE argument is not
 specified then use PRETTY_NAME from the file or the system name (see \\s).
 This escape code allows to keep \fI/etc/issue\fP distribution and release
 independent.  Note that \\S{ANSI_COLOR} is converted to the real terminal
@@ -402,7 +402,7 @@ the system status file.
 .B /etc/issue
 printed before the login prompt.
 .TP
-.B /etc/os-release /usr/lib/os-release
+.B /etc/os-release /lib/os-release
 operating system identification data.
 .TP
 .B /dev/console
diff --git a/term-utils/agetty.c b/term-utils/agetty.c
index 122959f..7f915d7 100644
--- a/term-utils/agetty.c
+++ b/term-utils/agetty.c
@@ -2491,7 +2491,7 @@ err:
  */
 static int plymouth_command(const char* arg)
 {
-	const char *cmd = "/usr/bin/plymouth";
+	const char *cmd = "/bin/plymouth";
 	static int has_plymouth = 1;
 	pid_t pid;
 
diff --git a/text-utils/more.c b/text-utils/more.c
index 4602cba..3435f12 100644
--- a/text-utils/more.c
+++ b/text-utils/more.c
@@ -1394,7 +1394,7 @@ int command(char *filename, register FILE *f)
 				 "/<regular expression>   Search for kth occurrence of regular expression [1]\n"
 				 "n                       Search for kth occurrence of last r.e [1]\n"
 				 "!<cmd> or :!<cmd>       Execute <cmd> in a subshell\n"
-				 "v                       Start up /usr/bin/vi at current line\n"
+				 "v                       Start up vi at current line\n"
 				 "ctrl-L                  Redraw screen\n"
 				 ":n                      Go to kth next file [1]\n"
 				 ":p                      Go to kth previous file [1]\n"
