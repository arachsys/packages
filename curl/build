mode autotools

check() {
  url https://curl.haxx.se/download/
  match curl 7.84.0 'curl-([0-9.]+)\.tar'

  url https://github.com/nghttp2/nghttp2/releases
  match nghttp2 1.48.0 'nghttp2-([0-9.]+)\.tar'
}

prepare() {
  unpack https://curl.haxx.se/download/curl-7.84.0.tar.xz
  unpack -d nghttp2 \
    https://github.com/nghttp2/nghttp2/releases/download/v1.48.0/nghttp2-1.48.0.tar.xz
  apply paths.diff
  apply sched.diff
  tree c6f85c7251574b64bbc9a393f41525547f67ecc341340e88e595f3d27415a2a7
}

pre-config() {
  cd /src/nghttp2
  /src/nghttp2/configure --disable-shared --enable-lib-only --with-pic
  make

  cd /src
  unexpand curl-config.in >curl-config.in.tmp
  mv -f curl-config.in{.tmp,}
  export PATH_SEPARATOR=:
}

config-extra --with-ca-bundle=/etc/ssl/cert.pem --with-openssl \
  CPPFLAGS="-DUSE_NGHTTP2=1 -I/src/nghttp2/lib/includes" \
  LDFLAGS=-L/src/nghttp2/lib/.libs LIBS=-lnghttp2

post-install() {
  rm -f /dst/bin/curl-config /dst/lib/aclocal/libcurl.m4
}
