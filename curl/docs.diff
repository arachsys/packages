diff --git a/Makefile.am b/Makefile.am
index b56ca1a..296c7f2 100644
--- a/Makefile.am
+++ b/Makefile.am
@@ -243,8 +243,6 @@ cygwinbin:
 # We extend the standard install with a custom hook:
 install-data-hook:
 	(cd include && $(MAKE) install)
-	(cd docs && $(MAKE) install)
-	(cd docs/libcurl && $(MAKE) install)
 
 # We extend the standard uninstall with a custom hook:
 uninstall-hook:
diff --git a/Makefile.in b/Makefile.in
index 95dd27e..8b4aaa7 100644
--- a/Makefile.in
+++ b/Makefile.in
@@ -1789,8 +1789,6 @@ cygwinbin:
 # We extend the standard install with a custom hook:
 install-data-hook:
 	(cd include && $(MAKE) install)
-	(cd docs && $(MAKE) install)
-	(cd docs/libcurl && $(MAKE) install)
 
 # We extend the standard uninstall with a custom hook:
 uninstall-hook:
diff --git a/src/Makefile.am b/src/Makefile.am
index 7a99c25..2c17dd7 100644
--- a/src/Makefile.am
+++ b/src/Makefile.am
@@ -45,8 +45,6 @@ AM_CPPFLAGS = -I$(top_srcdir)/include        \
 
 bin_PROGRAMS = curl
 
-SUBDIRS = ../docs
-
 if USE_CPPFLAG_CURL_STATICLIB
 AM_CPPFLAGS += -DCURL_STATICLIB
 endif
diff --git a/src/Makefile.in b/src/Makefile.in
index bf2ef68..81f43c2 100644
--- a/src/Makefile.in
+++ b/src/Makefile.in
@@ -746,7 +746,6 @@ AUTOMAKE_OPTIONS = foreign nostdinc
 AM_CPPFLAGS = -I$(top_srcdir)/include -I$(top_builddir)/lib \
 	-I$(top_builddir)/src -I$(top_srcdir)/lib -I$(top_srcdir)/src \
 	$(am__append_1) -DBUILDING_CURL
-SUBDIRS = ../docs
 
 # libcurl sources to include in curltool lib we use for test binaries
 CURLTOOL_LIBCURL_CFILES = \
