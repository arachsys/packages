diff --git a/src/rfc3315.c b/src/rfc3315.c
index 2ac6f86c..fbbc500c 100644
--- a/src/rfc3315.c
+++ b/src/rfc3315.c
@@ -176,22 +176,6 @@ static int dhcp6_maybe_relay(struct state *state, unsigned char *inbuff, size_t
 		    state->context = c;
 		  }
 	      }
-	  
-	  if (!state->context)
-	    {
-	      inet_ntop(AF_INET6, link_address, daemon->addrbuff, ADDRSTRLEN); 
-	      my_syslog(MS_DHCP | LOG_WARNING, 
-			_("no address range available for DHCPv6 request from relay at %s"),
-			daemon->addrbuff);
-	      return 0;
-	    }
-	}
-	  
-      if (!state->context)
-	{
-	  my_syslog(MS_DHCP | LOG_WARNING, 
-		    _("no address range available for DHCPv6 request via %s"), state->iface_name);
-	  return 0;
 	}
 
       state->relay_address = link_address;
