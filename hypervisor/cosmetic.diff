diff --git a/cloud-hypervisor/src/main.rs b/cloud-hypervisor/src/main.rs
index 0c9b3d12..0158ee3a 100644
--- a/cloud-hypervisor/src/main.rs
+++ b/cloud-hypervisor/src/main.rs
@@ -15,7 +15,7 @@ use std::path::PathBuf;
 use std::sync::mpsc::channel;
 use std::{any, cmp, env, io, num, process, str, thread};
 
-use clap::{Arg, ArgAction, ArgGroup, ArgMatches, Command};
+use clap::{Arg, ArgAction, ArgGroup, ArgMatches, Command, ColorChoice};
 use event_monitor::event;
 use libc::EFD_NONBLOCK;
 use log::{LevelFilter, error, info, warn};
@@ -70,11 +70,8 @@ fn handle_sigsys(info: &libc::siginfo_t) {
         let tid = libc::syscall(libc::SYS_gettid) as i64;
         let info = &*(info as *const libc::siginfo_t as *const SeccompSiginfo);
         eprintln!(
-            concat!(
-                "\n==== Possible seccomp violation ====\n",
-                "Syscall number: {} (arch: {:#x}, tid: {}, thread: {})\n",
-                "Try running with `strace -ff` to identify the cause and open an issue: ",
-                "https://github.com/cloud-hypervisor/cloud-hypervisor/issues/new",
+            concat!("Possible seccomp violation:\n",
+                "Syscall: {} (arch: {:#x}, tid: {}, thread: {})",
             ),
             info.si_syscall, info.si_arch, tid, thread_name,
         );
@@ -96,7 +93,7 @@ enum Error {
     CreateDebugEventFd(#[source] io::Error),
     #[error("Failed to create exit EventFd")]
     CreateExitEventFd(#[source] io::Error),
-    #[error("Failed to open hypervisor interface (is hypervisor interface available?)")]
+    #[error("Failed to open hypervisor interface")]
     CreateHypervisor(#[source] hypervisor::HypervisorError),
     #[error("Failed to start the VMM thread")]
     StartVmmThread(#[source] vmm::Error),
@@ -192,7 +189,7 @@ fn get_cli_options_sorted(
     [
         Arg::new("api-socket")
             .long("api-socket")
-            .help("HTTP API socket (UNIX domain socket): path=<path> or fd=<fd>.")
+            .help("Listen on HTTP API socket: path=<path>|fd=<fd>")
             .num_args(1)
             .group("vmm-config"),
         Arg::new("balloon")
@@ -202,7 +199,7 @@ fn get_cli_options_sorted(
             .group("vm-config"),
         Arg::new("cmdline")
             .long("cmdline")
-            .help("Kernel command line")
+            .help("Set kernel command line")
             .num_args(1)
             .group("vm-config"),
         Arg::new("console")
@@ -213,7 +210,7 @@ fn get_cli_options_sorted(
         Arg::new("cpus")
             .long("cpus")
             .help(
-                "boot=<boot_vcpus>,max=<max_vcpus>,\
+                "Set CPU parameters: boot=<boot_vcpus>,max=<max_vcpus>,\
                     topology=<threads_per_core>:<cores_per_die>:<dies_per_package>:<packages>,\
                     kvm_hyperv=on|off,max_phys_bits=<maximum_number_of_physical_bits>,\
                     affinity=<list_of_vcpus_with_their_associated_cpuset>,\
@@ -225,26 +222,26 @@ fn get_cli_options_sorted(
         #[cfg(feature = "dbus_api")]
         Arg::new("dbus-object-path")
             .long("dbus-object-path")
-            .help("Object path to serve the dbus interface")
+            .help("Set dbus object path")
             .num_args(1)
             .group("vmm-config"),
         #[cfg(feature = "dbus_api")]
         Arg::new("dbus-service-name")
             .long("dbus-service-name")
-            .help("Well known name of the device")
+            .help("Set dbus service name")
             .num_args(1)
             .group("vmm-config"),
         #[cfg(feature = "dbus_api")]
         Arg::new("dbus-system-bus")
             .long("dbus-system-bus")
             .action(ArgAction::SetTrue)
-            .help("Use the system bus instead of a session bus")
+            .help("Use the system bus not a session bus")
             .num_args(0)
             .group("vmm-config"),
         #[cfg(target_arch = "x86_64")]
         Arg::new("debug-console")
             .long("debug-console")
-            .help("Debug console: off|pty|tty|file=<path>,iobase=<port in hex>")
+            .help("Configure debug console: off|pty|tty|file=<path>,iobase=<port>")
             .default_value("off,iobase=0xe9")
             .group("vm-config"),
         Arg::new("device")
@@ -261,12 +258,12 @@ fn get_cli_options_sorted(
             .group("vm-config"),
         Arg::new("event-monitor")
             .long("event-monitor")
-            .help("Path to report events on: path=<path> or fd=<fd>")
+            .help("Report events in JSON format: path=<path>|fd=<fd>")
             .num_args(1)
             .group("vmm-config"),
         Arg::new("firmware")
             .long("firmware")
-            .help("Path to firmware that is loaded in an architectural specific way")
+            .help("Specify path to firmware image")
             .num_args(1)
             .group("vm-payload"),
         Arg::new("fs")
@@ -284,7 +281,7 @@ fn get_cli_options_sorted(
         #[cfg(feature = "guest_debug")]
         Arg::new("gdb")
             .long("gdb")
-            .help("GDB socket (UNIX domain socket): path=<path>")
+            .help("Listen on GDB socket: path=<path>")
             .num_args(1)
             .group("vmm-config"),
         Arg::new("generic-vhost-user")
@@ -296,18 +293,18 @@ fn get_cli_options_sorted(
         #[cfg(feature = "igvm")]
         Arg::new("igvm")
             .long("igvm")
-            .help("Path to IGVM file to load.")
+            .help("Provide IGVM file to SEV SNP guest")
             .num_args(1)
             .group("vm-payload"),
         #[cfg(feature = "sev_snp")]
         Arg::new("host-data")
             .long("host-data")
-            .help("Host specific data to SEV SNP guest")
+            .help("Provide host-specific data to SEV SNP guest")
             .num_args(1)
             .group("vm-config"),
         Arg::new("initramfs")
             .long("initramfs")
-            .help("Path to initramfs image")
+            .help("Specify path to initramfs image")
             .num_args(1)
             .group("vm-config"),
         #[cfg(feature = "ivshmem")]
@@ -318,16 +315,13 @@ fn get_cli_options_sorted(
             .group("vm-config"),
         Arg::new("kernel")
             .long("kernel")
-            .help(
-                "Path to kernel to load. This may be a kernel or firmware that supports a PVH \
-                entry point (e.g. vmlinux) or architecture equivalent",
-            )
+            .help("Specify path to kernel image")
             .num_args(1)
             .group("vm-payload"),
         Arg::new("landlock")
             .long("landlock")
             .num_args(0)
-            .help("enable/disable Landlock.")
+            .help("Enable/disable landlock")
             .action(ArgAction::SetTrue)
             .default_value("false")
             .group("vm-config"),
@@ -339,16 +333,14 @@ fn get_cli_options_sorted(
             .group("vm-config"),
         Arg::new("log-file")
             .long("log-file")
-            .help("Log file. Standard error is used if not specified")
+            .help("Redirect log output [default: stderr]")
             .num_args(1)
             .group("logging"),
         Arg::new("log-format")
             .long("log-format")
             .help(
-                "Log format. Common tokens: {boottime}, {wallclock}, {glog}, \
-                 {localglog}, {thread}, {level}, {location}, {msg}. See \
-                 docs/logging.md for the full list (per-field date/time tokens, \
-                 local-time variants, glog level letter).",
+                "Set log format: supports tokens: {boottime}, {wallclock}, {glog}, \
+                 {localglog}, {thread}, {level}, {location}, {msg}, and more",
             )
             .num_args(1)
             .default_value(logger::DEFAULT_FORMAT)
@@ -356,27 +348,27 @@ fn get_cli_options_sorted(
         Arg::new("memory")
             .long("memory")
             .help(
-                "Memory parameters \
-                     \"size=<guest_memory_size>,mergeable=on|off,shared=on|off,\
+                "Set memory parameters: \
+                     size=<guest_memory_size>,mergeable=on|off,shared=on|off,\
                      hugepages=on|off,hugepage_size=<hugepage_size>,\
                      hotplug_method=acpi|virtio-mem,\
                      hotplug_size=<hotpluggable_memory_size>,\
                      hotplugged_size=<hotplugged_memory_size>,\
-                     prefault=on|off,reserve=on|off,thp=on|off\"",
+                     prefault=on|off,reserve=on|off,thp=on|off",
             )
             .default_value(default_memory)
             .group("vm-config"),
         Arg::new("memory-zone")
             .long("memory-zone")
             .help(
-                "User defined memory zone parameters \
-                     \"size=<guest_memory_region_size>,file=<backing_file>,\
+                "Define memory zone: \
+                     size=<guest_memory_region_size>,file=<backing_file>,\
                      shared=on|off,\
                      hugepages=on|off,hugepage_size=<hugepage_size>,\
                      host_numa_node=<node_id>,\
                      id=<zone_identifier>,hotplug_size=<hotpluggable_memory_size>,\
                      hotplugged_size=<hotplugged_memory_size>,\
-                     prefault=on|off,reserve=on|off\"",
+                     prefault=on|off,reserve=on|off",
             )
             .num_args(1..)
             .action(ArgAction::Append)
@@ -389,7 +381,7 @@ fn get_cli_options_sorted(
             .group("vm-config"),
         Arg::new("no-shutdown")
             .long("no-shutdown")
-            .help("Do not exit the VMM when the guest shuts down")
+            .help("Do not exit on guest shutdown")
             .num_args(0)
             .action(ArgAction::SetTrue)
             .group("vmm-config"),
@@ -419,7 +411,7 @@ fn get_cli_options_sorted(
         #[cfg(feature = "pvmemcontrol")]
         Arg::new("pvmemcontrol")
             .long("pvmemcontrol")
-            .help("Pvmemcontrol device")
+            .help("Enable pvmemcontrol")
             .num_args(0)
             .action(ArgAction::SetTrue)
             .group("vm-config"),
@@ -453,6 +445,7 @@ fn get_cli_options_sorted(
             .group("vm-config"),
         Arg::new("seccomp")
             .long("seccomp")
+            .help("Enable/disable seccomp")
             .num_args(1)
             .value_parser(["true", "false", "log", "errno"])
             .default_value("true"),
@@ -474,8 +467,9 @@ fn get_cli_options_sorted(
             .group("vm-config"),
         Arg::new("v")
             .short('v')
+            .long("verbose")
             .action(ArgAction::Count)
-            .help("Sets the level of debugging output")
+            .help("Increase level of debugging output")
             .group("logging"),
         Arg::new("vdpa")
             .long("vdpa")
@@ -522,7 +516,8 @@ fn create_app(default_vcpus: String, default_memory: String, default_rng: String
         // 'BUILD_VERSION' is set by the build script 'build.rs' at
         // compile time
         .author(env!("CARGO_PKG_AUTHORS"))
-        .about("Launch a cloud-hypervisor VMM.")
+        .about("Launch a cloud-hypervisor VMM")
+        .color(ColorChoice::Never)
         .arg_required_else_help(true)
         .groups(groups)
         .args(args)
diff --git a/vmm/src/config.rs b/vmm/src/config.rs
index 900fd317..eb5e9134 100644
--- a/vmm/src/config.rs
+++ b/vmm/src/config.rs
@@ -808,8 +808,8 @@ impl CpusConfig {
 }
 
 impl PciSegmentConfig {
-    pub const SYNTAX: &'static str = "PCI Segment parameters \
-         \"pci_segment=<segment_id>,mmio32_aperture_weight=<scale>,mmio64_aperture_weight=<scale>\"";
+    pub const SYNTAX: &'static str = "Set PCI segment weights: \
+         pci_segment=<segment_id>,mmio32_aperture_weight=<scale>,mmio64_aperture_weight=<scale>";
 
     pub fn parse(disk: &str) -> Result<Self> {
         let mut parser = OptionParser::new();
@@ -868,8 +868,8 @@ impl PciSegmentConfig {
 impl PlatformConfig {
     pub fn syntax() -> &'static str {
         static SYNTAX: LazyLock<String> = LazyLock::new(|| {
-            let mut syntax = "Platform configuration parameters \
-            \"num_pci_segments=<num_pci_segments>,iommu_segments=<list_of_segments>,\
+            let mut syntax = "Set platform parameters: \
+            num_pci_segments=<num_pci_segments>,iommu_segments=<list_of_segments>,\
             iommu_address_width=<bits>,iommufd=on|off,iommufd_fd=<fd>,vfio_p2p_dma=on|off,\
             system_manufacturer=<dmi_system_manufacturer>,\
             system_product_name=<dmi_system_product_name>,system_version=<dmi_system_version>,\
@@ -886,8 +886,6 @@ impl PlatformConfig {
                 syntax.push_str(",sev_snp=on|off");
             }
 
-            syntax.push('"');
-
             syntax
         });
 
@@ -1276,10 +1274,10 @@ impl MemoryConfig {
 }
 
 impl RateLimiterGroupConfig {
-    pub const SYNTAX: &'static str = "Rate Limit Group parameters \
-        \"bw_size=<bytes>,bw_one_time_burst=<bytes>,bw_refill_time=<ms>,\
+    pub const SYNTAX: &'static str = "Define rate-limiter group: \
+        bw_size=<bytes>,bw_one_time_burst=<bytes>,bw_refill_time=<ms>,\
         ops_size=<io_ops>,ops_one_time_burst=<io_ops>,ops_refill_time=<ms>,\
-        id=<device_id>\"";
+        id=<device_id>";
 
     pub fn parse(rate_limit_group: &str) -> Result<Self> {
         let mut parser = OptionParser::new();
@@ -1426,8 +1424,8 @@ impl PciDeviceCommonConfig {
 }
 
 impl DiskConfig {
-    pub const SYNTAX: &'static str = "Disk parameters \
-         \"path=<disk_image_path>,readonly=on|off,direct=on|off,iommu=on|off,\
+    pub const SYNTAX: &'static str = "Add disk device: \
+         path=<disk_image>,readonly=on|off,direct=on|off,iommu=on|off,\
          num_queues=<number_of_queues>,queue_size=<size_of_each_queue>,\
          vhost_user=on|off,socket=<vhost_user_socket_path>,\
          bw_size=<bytes>,bw_one_time_burst=<bytes>,bw_refill_time=<ms>,\
@@ -1680,14 +1678,14 @@ impl FromStr for VhostMode {
 }
 
 impl NetConfig {
-    pub const SYNTAX: &'static str = "Network parameters \
-    \"tap=<if_name>,ip=<ip_addr>,mask=<net_mask>,mac=<mac_addr>,fd=<[fd1,fd2,...]>,iommu=on|off,\
+    pub const SYNTAX: &'static str = "Add network device: \
+    tap=<if_name>,ip=<ip_addr>,mask=<net_mask>,mac=<mac_addr>,fd=<[fd1,fd2,...]>,iommu=on|off,\
     num_queues=<number_of_queues>,queue_size=<size_of_each_queue>,id=<device_id>,\
     vhost_user=<vhost_user_enable>,socket=<vhost_user_socket_path>,vhost_mode=client|server,\
     bw_size=<bytes>,bw_one_time_burst=<bytes>,bw_refill_time=<ms>,\
     ops_size=<io_ops>,ops_one_time_burst=<io_ops>,ops_refill_time=<ms>,\
     pci_segment=<segment_id>,pci_device_id=<pci_slot>,\
-    offload_tso=on|off,offload_ufo=on|off,offload_csum=on|off\"";
+    offload_tso=on|off,offload_ufo=on|off,offload_csum=on|off";
 
     pub fn parse(net: &str) -> Result<Self> {
         let mut parser = OptionParser::new();
@@ -1902,9 +1900,9 @@ impl NetConfig {
 }
 
 impl RngConfig {
-    pub const SYNTAX: &'static str = "Random number generator parameters \"\
+    pub const SYNTAX: &'static str = "Add entropy device: \
         src=<entropy_source_path>,iommu=on|off,pci_segment=<segment_id>,\
-        pci_device_id=<pci_slot>\"";
+        pci_device_id=<pci_slot>";
 
     pub fn parse(rng: &str) -> Result<Self> {
         let mut parser = OptionParser::new();
@@ -1952,9 +1950,9 @@ impl RtcConfig {
 }
 
 impl BalloonConfig {
-    pub const SYNTAX: &'static str = "Balloon parameters \"size=<balloon_size>,deflate_on_oom=on|off,\
+    pub const SYNTAX: &'static str = "Add balloon device: size=<balloon_size>,deflate_on_oom=on|off,\
         free_page_reporting=on|off,iommu=on|off,id=<device_id>,pci_segment=<segment_id>,\
-        pci_device_id=<pci_slot>\"";
+        pci_device_id=<pci_slot>";
 
     pub fn parse(balloon: &str) -> Result<Self> {
         let mut parser = OptionParser::new();
@@ -1997,11 +1995,11 @@ impl BalloonConfig {
 }
 
 impl GenericVhostUserConfig {
-    pub const SYNTAX: &'static str = "generic vhost-user parameters \
-    \"device_type=<ID number for virtio device type (FS, block, net, etc) or symbolic name>,\
+    pub const SYNTAX: &'static str = "Add generic vhost-user device: \
+    device_type=<device_type>,\
     socket=<socket_path>,\
-    queue_sizes=<list of queue sizes>,\
-    id=<device_id>,pci_segment=<segment_id>,pci_device_id=<pci_slot>\"";
+    queue_sizes=<list_of_queue_sizes>,\
+    id=<device_id>,pci_segment=<segment_id>,pci_device_id=<pci_slot>";
 
     pub fn parse(vhost_user: &str) -> Result<Self> {
         let mut parser = OptionParser::new();
@@ -2125,10 +2123,10 @@ impl GenericVhostUserConfig {
 }
 
 impl FsConfig {
-    pub const SYNTAX: &'static str = "virtio-fs parameters \
-    \"tag=<tag_name>,socket=<socket_path>,num_queues=<number_of_queues>,\
+    pub const SYNTAX: &'static str = "Add filesystem device: \
+    tag=<tag_name>,socket=<socket_path>,num_queues=<number_of_queues>,\
     queue_size=<size_of_each_queue>,id=<device_id>,\
-    pci_segment=<segment_id>,pci_device_id=<pci_slot>\"";
+    pci_segment=<segment_id>,pci_device_id=<pci_slot>";
 
     pub fn parse(fs: &str) -> Result<Self> {
         let mut parser = OptionParser::new();
@@ -2186,9 +2184,9 @@ impl FsConfig {
 
 #[cfg(feature = "fw_cfg")]
 impl FwCfgConfig {
-    pub const SYNTAX: &'static str = "Boot params to pass to FW CFG device \
-    \"e820=on|off,kernel=on|off,cmdline=on|off,initramfs=on|off,acpi_table=on|off, \
-    items=[name=<item_name>,file=<file_path>:name=<item_name>,string=<string_value>]\"";
+    pub const SYNTAX: &'static str = "Enable fw_cfg device: \
+    e820=on|off,kernel=on|off,cmdline=on|off,initramfs=on|off,acpi_table=on|off, \
+    items=[name=<item_name>,file=<file_path>:name=<item_name>,string=<string_value>]";
     pub fn parse(fw_cfg_config: &str) -> Result<Self> {
         let mut parser = OptionParser::new();
         parser
@@ -2285,10 +2283,10 @@ impl FwCfgItem {
 }
 
 impl PmemConfig {
-    pub const SYNTAX: &'static str = "Persistent memory parameters \
-    \"file=<backing_file_path>,size=<persistent_memory_size>,iommu=on|off,\
+    pub const SYNTAX: &'static str = "Add persistent memory device: \
+    file=<backing_file_path>,size=<persistent_memory_size>,iommu=on|off,\
     discard_writes=on|off,id=<device_id>,\
-    pci_segment=<segment_id>,pci_device_id=<pci_slot>\"";
+    pci_segment=<segment_id>,pci_device_id=<pci_slot>";
 
     pub fn parse(pmem: &str) -> Result<Self> {
         let mut parser = OptionParser::new();
@@ -2453,11 +2451,11 @@ impl DebugConsoleConfig {
 }
 
 impl DeviceConfig {
-    pub const SYNTAX: &'static str = "Direct device assignment parameters \
-    \"path=<device_path>,fd=<vfio_cdev_fd>,iommu=on|off,id=<device_id>,\
+    pub const SYNTAX: &'static str = "Assign pass-through device to guest: \
+    path=<device_path>,fd=<vfio_cdev_fd>,iommu=on|off,id=<device_id>,\
     pci_segment=<segment_id>,pci_device_id=<pci_slot>,\
     x_nv_gpudirect_clique=<clique_id>,\
-    x_exclude_mmap_bars=[<bar>...]\"";
+    x_exclude_mmap_bars=[<bar>...]";
 
     pub fn parse(device: &str) -> Result<Self> {
         let mut parser = OptionParser::new();
@@ -2526,8 +2524,8 @@ impl DeviceConfig {
 }
 
 impl UserDeviceConfig {
-    pub const SYNTAX: &'static str = "Userspace device socket=<socket_path>,id=<device_id>,\
-        pci_segment=<segment_id>,pci_device_id=<pci_slot>\"";
+    pub const SYNTAX: &'static str = "Add userspace device: socket=<socket_path>,id=<device_id>,\
+        pci_segment=<segment_id>,pci_device_id=<pci_slot>";
 
     pub fn parse(user_device: &str) -> Result<Self> {
         let mut parser = OptionParser::new();
@@ -2553,9 +2551,9 @@ impl UserDeviceConfig {
 }
 
 impl VdpaConfig {
-    pub const SYNTAX: &'static str = "vDPA device \
-        \"path=<device_path>,num_queues=<number_of_queues>,iommu=on|off,\
-        id=<device_id>,pci_segment=<segment_id>,pci_device_id=<pci_slot>\"";
+    pub const SYNTAX: &'static str = "Add vDPA device: \
+        path=<device_path>,num_queues=<number_of_queues>,iommu=on|off,\
+        id=<device_id>,pci_segment=<segment_id>,pci_device_id=<pci_slot>";
 
     pub fn parse(vdpa: &str) -> Result<Self> {
         let mut parser = OptionParser::new();
@@ -2588,9 +2586,9 @@ impl VdpaConfig {
 }
 
 impl VsockConfig {
-    pub const SYNTAX: &'static str = "Virtio VSOCK parameters \
-        \"cid=<context_id>,socket=<socket_path>,iommu=on|off,id=<device_id>,\
-        pci_segment=<segment_id>,pci_device_id=<pci_slot>\"";
+    pub const SYNTAX: &'static str = "Add vsock device: \
+        cid=<context_id>,socket=<socket_path>,iommu=on|off,id=<device_id>,\
+        pci_segment=<segment_id>,pci_device_id=<pci_slot>";
 
     pub fn parse(vsock: &str) -> Result<Self> {
         let mut parser = OptionParser::new();
@@ -2623,11 +2621,11 @@ impl VsockConfig {
 }
 
 impl NumaConfig {
-    pub const SYNTAX: &'static str = "Settings related to a given NUMA node \
-        \"guest_numa_id=<node_id>,cpus=<cpus_id>,distances=<list_of_distances_to_destination_nodes>,\
+    pub const SYNTAX: &'static str = "Set NUMA parameters: \
+        guest_numa_id=<node_id>,cpus=<cpus_id>,distances=<list_of_distances_to_destination_nodes>,\
         device_id=<device_id>,\
         memory_zones=<list_of_memory_zones>,\
-        pci_segments=<list_of_pci_segments>\"";
+        pci_segments=<list_of_pci_segments>";
 
     pub fn parse(numa: &str) -> Result<Self> {
         let mut parser = OptionParser::new();
@@ -2816,15 +2814,9 @@ pub struct RestoreConfig {
 }
 
 impl RestoreConfig {
-    pub const SYNTAX: &'static str = "Restore from a VM snapshot. \
-        \nRestore parameters \"source_url=<source_url>,prefault=on|off,memory_restore_mode=copy|ondemand,\
-        net_fds=<list_of_net_ids_with_their_associated_fds>,resume=true|false\" \
-        \n`source_url` should be a valid URL (e.g file:///foo/bar or tcp://192.168.1.10/foo) \
-        \n`prefault` controls eager prefaulting for the copy-based restore path (disabled by default) \
-        \n`memory_restore_mode=copy` preserves the existing eager read-copy restore behavior, while `memory_restore_mode=ondemand` enables lazy demand paging and fails restore if userfaultfd support is unavailable \
-        \n`net_fds` is a list of net ids with new file descriptors. \
-        Only net devices backed by FDs directly are needed as input.\
-        \n `resume` controls whether the VM will be directly resumed after restore ";
+    pub const SYNTAX: &'static str = "Restore from snapshot: \
+        source_url=<source_url>,prefault=on|off,memory_restore_mode=copy|ondemand,\
+        net_fds=<list_of_net_ids_with_their_associated_fds>,resume=true|false";
 
     pub fn parse(restore: &str) -> Result<Self> {
         let mut parser = OptionParser::new();
@@ -2928,8 +2920,8 @@ impl RestoreConfig {
 }
 
 impl TpmConfig {
-    pub const SYNTAX: &'static str = "TPM device \
-        \"(UNIX Domain Socket from swtpm) socket=</path/to/a/socket>\"";
+    pub const SYNTAX: &'static str = "Add TPM device: \
+        socket=<path>";
 
     pub fn parse(tpm: &str) -> Result<Self> {
         let mut parser = OptionParser::new();
@@ -2944,8 +2936,8 @@ impl TpmConfig {
 }
 
 impl LandlockConfig {
-    pub const SYNTAX: &'static str = "Landlock parameters \
-        \"path=<path/to/{file/dir}>,access=[rw]\"";
+    pub const SYNTAX: &'static str = "Add landlock rules: \
+        path=<path>,access=[rw]";
 
     pub fn parse(landlock_rule: &str) -> Result<Self> {
         let mut parser = OptionParser::new();
@@ -2984,11 +2976,10 @@ impl LandlockConfig {
 
 #[cfg(feature = "ivshmem")]
 impl IvshmemConfig {
-    pub const SYNTAX: &'static str = "Ivshmem device. Specify the backend file path and size \
-    for the shared memory: \"path=</path/to/a/file>,size=<file_size>,id=<device_id>,\
-    pci_segment=<segment_id>,pci_device_id=<pci_slot>\" \
-    \nThe <file_size> must be a power of 2 (e.g., 2M, 4M, etc.), as it represents the size \
-    of the memory region mapped to the guest. Default size is 128M.";
+    pub const SYNTAX: &'static str = "Add inter-VM shared memory device: \
+    path=<path>,size=<size>,id=<device_id>,\
+    pci_segment=<segment_id>,pci_device_id=<pci_slot>\
+    \n<size> must be a power of 2 [default: 128M]";
     pub fn parse(ivshmem: &str) -> Result<Self> {
         let mut parser = OptionParser::new();
         parser.add("path").add("size");
diff --git a/vmm/src/vm_config.rs b/vmm/src/vm_config.rs
index 04102478..ac6336bc 100644
--- a/vmm/src/vm_config.rs
+++ b/vmm/src/vm_config.rs
@@ -685,7 +685,7 @@ pub struct SerialConfig {
 }
 
 impl SerialConfig {
-    pub const SYNTAX: &str = "Control serial port: \"off|null|pty|tty|file=<path>|socket=<path>\"";
+    pub const SYNTAX: &str = "Configure serial port: off|null|pty|tty|file=<path>|socket=<path>";
 }
 
 impl Default for SerialConfig {
@@ -716,7 +716,7 @@ pub struct ConsoleConfig {
 }
 
 impl ConsoleConfig {
-    pub const SYNTAX: &str = "Control (virtio) console: \"off|null|pty|tty|file=<path>,iommu=on|off,id=<device_id>,pci_segment=<segment_id>,pci_device_id=<pci_slot>\"";
+    pub const SYNTAX: &str = "Configure virtio console: off|null|pty|tty|file=<path>,iommu=on|off,id=<device_id>,pci_segment=<segment_id>,pci_device_id=<pci_slot>";
 }
 
 impl Default for ConsoleConfig {
