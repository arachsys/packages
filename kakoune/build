check() {
  url https://github.com/mawww/kakoune/tags
  match version 2024.05.18 'tags/v([0-9.]+)\.tar'
}

prepare() {
   unpack kakoune-2024.09.16.tar.gz \
     https://github.com/mawww/kakoune/archive/daa7e37e.tar.gz
  apply init.diff
  apply musl.diff
  apply news.diff
  apply palette.diff
  apply paths.diff
  apply perl.diff
  apply tabs.diff
  apply windowing.diff
  find -xtype l -delete
  tree 44ae3dc524f203b63ca85ed8e4c706553489aab56fde1611cc40fcbfe7beac34
}

build() {
  if enabled static; then
    if [[ $(c++ --version) == *clang* ]]; then
      LIBS='LDFLAGS+=-static LIBS+=-lunwind LIBS+=-lc++abi'
    else
      LIBS='LDFLAGS+=-static'
    fi
  fi

  make install DESTDIR=/dst PREFIX= version=2024.09.16 $LIBS
  find /dst/lib/kak/{colors,kakrc,rc} -type f | while read FILE; do
    sed -E -e 's/rgb:[[:xdigit:]]{2}{3,4}/\L&/g' -e '/^\s*#/d' \
        -e "s/^(([^'\"#]*|'[^']*'|\"([^\"\\]|\\\\.)*\")*)\\s+#\\s.*/\\1/" \
        -e 's/\s+$//' -e '/./,/^$/p' -n "$FILE" | sed '${/^$/d}' \
      | expand -i > "$FILE.tmp" && mv -f "$FILE.tmp" "$FILE"
  done
  rm -f /dst/lib/kak/rc/{*/*.pl,tools/{git,man,patch}.kak}
}
