diff --git a/src/normal.cc b/src/normal.cc
index d86ea3e..4fdd2fa 100644
--- a/src/normal.cc
+++ b/src/normal.cc
@@ -190,8 +190,13 @@ String build_autoinfo_for_mapping(const Context& context, KeymapMode mode,
     }
 
     for (auto& key : keymaps.get_mapped_keys(mode))
+    {
+        if (keymaps.get_mapping_keys(key, mode).empty())
+            if (keymaps.get_mapping_docstring(key, mode).empty())
+                continue;
         descs.emplace_back(to_string(key),
                            keymaps.get_mapping_docstring(key, mode));
+    }
 
     auto max_len = 0_col;
     for (auto& desc : descs)
