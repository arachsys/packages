diff --git a/Makefile b/Makefile
index 2458a4abc..df679fa23 100644
--- a/Makefile
+++ b/Makefile
@@ -419,6 +419,9 @@ HOST_LFS_CFLAGS := $(shell getconf LFS_CFLAGS 2>/dev/null)
 HOST_LFS_LDFLAGS := $(shell getconf LFS_LDFLAGS 2>/dev/null)
 HOST_LFS_LIBS := $(shell getconf LFS_LIBS 2>/dev/null)
 
+LLVM = 1
+LLVM_IAS = 1
+
 ifneq ($(LLVM),)
 HOSTCC	= clang
 HOSTCXX	= clang++
@@ -440,7 +443,7 @@ KBUILD_HOSTLDLIBS   := $(HOST_LFS_LIBS) $(HOSTLDLIBS)
 CPP		= $(CC) -E
 ifneq ($(LLVM),)
 CC		= clang
-LD		= ld.lld
+LD		= ld
 AR		= llvm-ar
 NM		= llvm-nm
 OBJCOPY		= llvm-objcopy
diff --git a/tools/scripts/Makefile.include b/tools/scripts/Makefile.include
index 071312f5e..f9b1de39c 100644
--- a/tools/scripts/Makefile.include
+++ b/tools/scripts/Makefile.include
@@ -71,7 +71,7 @@ CC_NO_CLANG := $(shell $(CC) -dM -E -x c /dev/null | grep -Fq "__clang__"; echo
 ifneq ($(LLVM),)
 HOSTAR  ?= llvm-ar
 HOSTCC  ?= clang
-HOSTLD  ?= ld.lld
+HOSTLD  ?= ld
 else
 HOSTAR  ?= ar
 HOSTCC  ?= gcc
