diff --git a/vendor/curl-sys-0.4.79+curl-8.12.0/build.rs b/vendor/curl-sys-0.4.79+curl-8.12.0/build.rs
index 8315a20f..34c6d3e2 100644
--- a/vendor/curl-sys-0.4.79+curl-8.12.0/build.rs
+++ b/vendor/curl-sys-0.4.79+curl-8.12.0/build.rs
@@ -35,6 +35,11 @@ fn main() {
             return println!("cargo:rustc-flags=-l curl");
         }
 
+        // Force the system libcurl on Linux
+        if target.contains("linux") {
+            return println!("cargo:rustc-flags=-l curl");
+        }
+
         // Next, fall back and try to use pkg-config if its available.
         if windows {
             if try_vcpkg() {
diff --git a/vendor/curl-sys-0.4.87+curl-8.19.0/build.rs b/vendor/curl-sys-0.4.87+curl-8.19.0/build.rs
index 48712723..3be54e4f 100644
--- a/vendor/curl-sys-0.4.87+curl-8.19.0/build.rs
+++ b/vendor/curl-sys-0.4.87+curl-8.19.0/build.rs
@@ -35,6 +35,11 @@ fn main() {
             return println!("cargo:rustc-flags=-l curl");
         }
 
+        // Force the system libcurl on Linux
+        if target.contains("linux") {
+            return println!("cargo:rustc-flags=-l curl");
+        }
+
         // Next, fall back and try to use pkg-config if its available.
         if windows {
             if try_vcpkg() {
diff --git a/vendor/curl-sys-0.4.90+curl-8.21.0/build.rs b/vendor/curl-sys-0.4.90+curl-8.21.0/build.rs
index 5ae0ed8e..359e2987 100644
--- a/vendor/curl-sys-0.4.90+curl-8.21.0/build.rs
+++ b/vendor/curl-sys-0.4.90+curl-8.21.0/build.rs
@@ -35,6 +35,11 @@ fn main() {
             return println!("cargo:rustc-flags=-l curl");
         }
 
+        // Force the system libcurl on Linux
+        if target.contains("linux") {
+            return println!("cargo:rustc-flags=-l curl");
+        }
+
         // Next, fall back and try to use pkg-config if its available.
         if windows {
             if try_vcpkg() {
