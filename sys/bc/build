name bc
version 1.06.95
mode autotools
sources http://alpha.gnu.org/gnu/bc/bc-$VERSION.tar.bz2 leak.patch
requires -b flex
enabled docs && requires -b texinfo

pre-config() {
  enabled docs || sed -e '/^SUBDIRS =/s/ doc//' -i Makefile.{am,in}
}
