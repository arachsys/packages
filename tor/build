require libevent

check() {
  url https://dist.torproject.org/
  match version 0.4.7.13 'tor-([0-9.]+)\.tar'
}

prepare() {
  unpack https://dist.torproject.org/tor-0.4.7.13.tar.gz
  apply cosmetic.diff
  apply config.diff
  apply libressl.diff
  tree ffe1c3b6693734bbe68068575027599dee89840f0cdac5e0c4571c9b7076c8bf
}

build() {
  auto-config --enable-lzma --enable-zstd CONFDIR=/etc \
    {LZMA,ZSTD}_CFLAGS=-I/lib/include LZMA_LIBS=-llzma ZSTD_LIBS=-lzstd
  sed -i 's:-levent:/lib/static/libevent.a:g' Makefile
  make install DESTDIR=/dst
  rm -f /dst/bin/torify
}
